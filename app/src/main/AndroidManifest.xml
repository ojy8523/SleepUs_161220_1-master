<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.seungkyu.dreamtodream">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <uses-sdk tools:overrideLibrary="com.betomaluje.android.miband"/>

    <application
        android:allowBackup="true"
        tools:replace="android:icon,android:theme"
        android:icon="@drawable/ic_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Signup" />
        <activity android:name=".CompanyList" />
        <activity android:name=".CompanyViewPage" />
        <activity android:name=".StartCharity" />
        <activity android:name=".SplashPage" />
        <activity android:name=".CharityBoardViewPage"/>
        <activity android:name=".CharityRanking"/>
        <activity android:name=".Board" />
        <activity android:name=".WriteBoardPage"/>
        <activity android:name=".BoardViewPage">
        </activity>



        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />


    </application>

</manifest>